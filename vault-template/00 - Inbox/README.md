# Inbox

Capture everything here first. The agent (or you) will sort it later.

Naming convention: `YYYYMMDD-NNN-<short title>.md`

This README acts as a `.gitkeep` so the empty directory is preserved in git.

# QuickCapture Plugin

Optional capture entrypoint plugin.

Current compatibility entrypoints:

- `tools/setup/install_quickcapture.py`
- `tools/scripts/quickcapture/*`

This plugin must not be hard-bound to LLM-GTD. The destination should be configurable over time: GTD Inbox, knowledge inbox, or another vault entrypoint.


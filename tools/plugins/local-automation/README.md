# Local Automation Plugin

Optional local scheduled-job plugin.

Current compatibility entrypoint:

- `tools/setup/create_launchd.py`

This plugin owns local machine automation such as refresh and snapshot jobs. It must be installed only when the user chooses local automation.


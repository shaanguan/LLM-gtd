# LLM-GTD 项目审计报告

> 审计时间：2026-06-11
> 审计范围：`/Users/zhoubo/Open GTD/llm-gtd/` 全仓库
> 审计方法：逐文件阅读源码 + 架构分析 + 交叉一致性检查

---

## 项目总览

**LLM-GTD** 是一套 AI 驱动的 GTD（Getting Things Done）系统，以 Obsidian Vault（Markdown 文件）为唯一数据源，通过 QoderWork 运行 AI Agent 实现全自动化的任务捕获、处理、回顾和渲染。

### 架构

```
渲染层  Dashboard.html │ 钉钉每日安排 │ 钉钉排期表
          ↑ export_dashboard.py / DingTalk MCP
Agent 层  QoderWork + AGENTS.md（17 节操作规范）+ 知识库
          ↑ 读/写/移动/归档
存储层  Obsidian Vault（00-Inbox ~ 07-Achievements + Scripts + Templates）
```

### 核心设计理念

- **Vault 是唯一数据源**，Agent 是唯一操作者，用户只看渲染层输出
- **AGENTS.md 即大脑**——注入到每次 Agent 会话，修改即时生效
- **条件渲染**的功能开关（`<!-- IF feature.X -->`）让模板按需裁剪
- **知识库与操作分离**——GTD 方法论 wiki 共享，vault 是个人的

### 架构亮点

- 三层分离设计清晰，关注点分离做得好
- §15/§16 把真实踩过的坑（Wiki 被覆盖、日期断言错误、图片尺寸丢失）记录为防御规则，非常务实
- 渲染层按受众分粒度（Dashboard 全量、排期表仅交付件、日报仅 MIT），同源不同视图

---

## 🔴 P0 — 核心问题

### 1. 缺少自动化测试

整个项目有 5 个 Python 脚本 + `init.py` + `doctor.py` + `export_dashboard.py`，但**零测试**。这些脚本直接操作用户数据（vault 文件），一旦出 bug 影响严重。

**建议：**
- 给 `export_dashboard.py` 的 `parse_frontmatter`、`build_item`、`is_side_project` 加单测
- 给 `init.py` 的 `render_conditionals`、`render_placeholders` 加单测（模板引擎核心）
- 给 `verify_sync.py` 的 `check_dup_inbox`（bigram jaccard）加边界 case 测试
- 用 `pytest` + 临时目录 fixture，成本很低

### 2. `export_dashboard.py` 的数据注入方式脆弱

```python
pattern = rf"const {name}=.*?;(?=\s*\n)"
```

该正则依赖 `const DATA=...;` 必须在一行内、以 `;` + 换行结尾。一旦 Dashboard.html 被格式化工具处理（Prettier 等），或 DATA JSON 中含 `;\n` 字符串，就会断掉。

**建议：**
- 改用明确的标记注释作为边界，如 `/* __DATA_START__ */` 和 `/* __DATA_END__ */`
- 或将数据写入独立的 `dashboard-data.json`，Dashboard.html 通过 `<script src>` 加载

### 3. 缺少依赖管理文件

PyYAML 是必需依赖（`export_dashboard.py` 直接 `import yaml` 不带 try/except），但项目里**没有 `requirements.txt` 或 `pyproject.toml`**。用户运行 `export_dashboard.py` 时可能直接报 ImportError。

**建议：**
- 添加 `requirements.txt`（至少 `pyyaml>=6.0`）
- `doctor.py` 中增加 PyYAML 安装检查（当前仅在 `check_config_yaml` 里有 try/except，但主数据导出脚本没有）

---

## 🟡 P1 — 重要改进

### 4. AGENTS.md 超出自身设定的行数上限

AGENTS.md 声明"≤ 400 lines / ≤ 18 KB"是 context sweet spot，但实际为 **512 行**。长文件注入到每次 Agent 会话，消耗宝贵的 token 预算。

**建议：**
- §4 DingTalk 部分（约 100 行）移到 `05 - Reference/dingtalk-ops.md`，AGENTS.md 只留锚点
- §16 Lessons Learned 移到 Reference
- 目标压缩到 350 行以内

### 5. Inbox SLA 依赖文件 mtime，不可靠

`inbox_sla.py` 用 `f.stat().st_mtime` 判断 Inbox 文件年龄。但 git clone、文件同步（Dropbox/iCloud）、Obsidian 插件重写等操作都会重置 mtime。

**建议：**
- 改用 frontmatter 中的 `date` 或 `created` 字段作为创建时间
- `mtime` 作为 fallback

### 6. `init.py --non-interactive` 不收集关键参数

非交互模式下 `user_name = "User"`、`user_role = "Knowledge Worker"`，生成的 AGENTS.md 包含占位符式通用文本。SKILL.md 中的自动化 setup 流程会走此路径。

**建议：**
- 增加 `--name` 和 `--role` CLI 参数
- `--non-interactive` 模式下要求通过参数传入

### 7. Dashboard `file://` 协议限制

`file://` 打开 HTML 在不同浏览器/OS 上有安全限制（CORS、localStorage 等），`fLink()` 生成的 `file://` 链接在非 macOS 环境可能不生效。

**建议：**
- 短期：README 明确推荐浏览器及已知限制
- 中期：提供 `serve_dashboard.py`（一行 `python3 -m http.server`）

---

## 🟢 P2 — 锦上添花

### 8. 知识库同步脚本缺文档

`scripts/sync-knowledge.sh` 没有用法说明、源目录格式要求、幂等性保证等文档。

### 9. Demo vault 缺少示例数据

`examples/demo-vault/` 各目录下无 `.md` 示例文件。README 提到"虚构设计师 Li Wei"但无实际数据，无法即开即用。

### 10. 错误处理对 Agent 调用不友好

`_config.py` 的 `get_vault()` 在 `$GTD_VAULT` 未设时直接 `sys.exit(2)`，对被 Agent 调用的场景应抛异常而非退出进程。建议提供 `get_vault_or_none()` 变体。

### 11. 缺少版本号管理

SKILL.md 有 `version: 1.0.0`，但项目本身无统一版本号（无 `__version__`、无 git tag）。模板仓库 + 工具链的升级兼容性需要版本号。

### 12. Dashboard 导出泄露本地路径

`export_dashboard.py` 把 vault 绝对路径 (`VBASE`) 注入 HTML 文件。用户分享 Dashboard.html 会泄露本地路径。建议提供 `--no-vbase` 选项或在 README 提醒。

---

## 📐 代码质量小项

| 位置 | 问题 | 建议 |
|------|------|------|
| `init.py` L128 | `cron.daily_doc_time` 的 `.replace(':30', ':00').replace(':00', ':00')` 第二个 replace 是空操作 | 删掉或修正逻辑 |
| `verify_sync.py` | 报告写到 `/tmp/gtd-sync-report.md` 硬编码路径 | 改为 `$GTD_VAULT/.llm-gtd/state/` |
| `_config.py` | `DEFAULTS` 中 weekly schedule 是 `"Fri 18:30"` | 与 README/SKILL 默认 `"Sun 21:00"` 不一致，需统一 |
| `config.schema.yaml` | weekly schedule 同样写的 `"Fri 18:30"` | 与代码和文档统一 |

---

## 🏗️ 架构演进建议

1. **多 vault 支持** — 当前强绑定单一 `$GTD_VAULT`，如果用户想区分工作/生活两个独立 vault，架构需要扩展
2. **移动端捕获** — 仅有 Obsidian 和对话两个入口，可提供 iOS Shortcut / Android Tasker 模板直接往 Inbox 写文件
3. **Dashboard 交互化** — 当前纯展示，未来可加"标记完成"按钮（通过写文件实现），减少对话成本
4. **Obsidian 插件** — 提供轻量插件触发 `export_dashboard.py`，不依赖 Agent cron

---

## 总结

| 维度 | 评价 |
|------|------|
| **架构设计** | ⭐⭐⭐⭐⭐ 三层分离 + AGENTS.md 即时生效 + 渲染层受众分粒度，设计成熟 |
| **实用性** | ⭐⭐⭐⭐⭐ 真实使用迭代出的产品，Lessons Learned 记录了 4 个真实事故 |
| **代码质量** | ⭐⭐⭐⭐ 代码简洁可读，但有少量一致性问题 |
| **工程基础** | ⭐⭐⭐ 缺少测试、依赖管理、版本号 |
| **文档** | ⭐⭐⭐⭐ README/架构文档/用户指南齐全，部分脚本缺文档 |
| **安全性** | ⭐⭐⭐⭐ 红线规则明确，数据全本地；路径泄露和 mtime 依赖需注意 |

**核心结论**：这是一个**设计思路非常成熟**的项目，从 AGENTS.md 的 17 节规范和事故复盘可以看出是真实落地并迭代出来的。主要短板在**工程基础设施**（测试、依赖管理、版本号）和**细节一致性**上。核心架构没有硬伤，值得继续投入。
AUDIT_EOF; __aone_exit=$?; pwd -P > '/var/folders/hn/6xsdmlm91rq_49l_24gs91040000gp/T/aone-copilot-cwd-1781173361174-dkc06sw2409.txt' 2>/dev/null; exit $__aone_exit